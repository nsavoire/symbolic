[package]
name = "symbolic-cabi"
version = "12.9.2-dd"
license = "MIT"
authors = [
    "Armin Ronacher <armin.ronacher@active-4.com>",
    "Jan Michael Auer <mail@jauer.org>",
]
homepage = "https://github.com/getsentry/symbolic"
repository = "https://github.com/getsentry/symbolic"
description = """
C interface wrapper for symbolic, a library to symbolicate and process stack
traces from native applications, minidumps, minified JavaScript or ProGuard
optimized Android apps.
"""
edition = "2021"
publish = false

[lib]
crate-type = ["staticlib"]

[dependencies]
proguard = { workspace = true, features = ["uuid"] }
anyhow = "1.0.32"
sourcemap = { workspace = true }
symbolic = { version = "12.9.2-dd", path = "../symbolic", features = [
    "cfi",
    "debuginfo",
    "demangle",
    "il2cpp",
    "sourcemapcache",
    "symcache",
] }
tempfile = { workspace = true }
